import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'thonburi_travel',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
